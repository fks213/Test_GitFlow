
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ИКЗ = Запись.ИсходныйКлючЗаписи;	
	ОбновлятьДанныеВладельца = НЕ ИКЗ.Пустой();
	Элементы.Номенклатура.Доступность	= ИКЗ.Пустой();
	Элементы.ЛичныйКабинет.Доступность	= ИКЗ.Пустой();
КонецПроцедуры

&НаКлиенте
Процедура ПриЗакрытии(ЗавершениеРаботы)
	
КонецПроцедуры

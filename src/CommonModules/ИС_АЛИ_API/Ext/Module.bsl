
#Область ПрограммныйИнтерфейс

#Область Цены                        

//+ ИнфоСофт Стояк Ю.К. 10.02.2023 УА #54628
Функция ВыгрузитьЦены(Профиль, ПараметрыЗапроса, СтруктураТела) Экспорт

	ПараметрыЗапроса.Заголовки.Вставить("Content-Type",   "application/json");		              
	ПараметрыЗапроса.АдресРесурса = "/api/v1/product/update-sku-price";
	ПараметрыЗапроса.ФорматОтвета = "Строка";
	
	ТелоЗапроса = Новый Структура;
	ТелоЗапроса.Вставить("products", СтруктураТела);
	
	ПараметрыЗапроса.ТелоСтрока = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьСтрокуJSON(ТелоЗапроса);
		
	РезультатЗапроса = ИС_MP_РаботаСHTTPЗапросамиСервер.ОтправитьPOSTЗапрос(ПараметрыЗапроса);
	
	ЗаписатьДанныеОтладки(Профиль, Перечисления.ИС_МП_МетодыHTTP.POST, ПараметрыЗапроса, РезультатЗапроса);
	
	Если РезультатЗапроса.ЕстьОшибки Тогда
		Текст = СтрШаблон("Произошла ошибка при отправке запроса: %1",	РезультатЗапроса.ИнформацияОбОшибке.Описание);
		ОбщегоНазначения.СообщитьПользователю(Текст); 
		Возврат Неопределено;	
	Иначе
	
		Если РезультатЗапроса.КодОтвета = 200 Тогда
		
			JSON = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьОбъектJSON(РезультатЗапроса.Ответ);
			
			Если ТипЗнч(JSON) = Тип("Структура") Тогда				
				Возврат JSON.results[0].ok;	
			Иначе
				Текст = СтрШаблон("Получен нераспознанный ответ от сервера: %1", РезультатЗапроса.Ответ);
			 	ОбщегоНазначения.СообщитьПользователю(Текст);
				Возврат Неопределено;
			КонецЕсли;
			
		ИначеЕсли РезультатЗапроса.КодОтвета = 400 Тогда
             
            Текст = СтрШаблон("Неверная форма запроса: %1", РезультатЗапроса.Ответ); 
      		ОбщегоНазначения.СообщитьПользователю(Текст);
			Возврат Неопределено;
			
		Иначе
			
			РазобратьHTTPОтветПоКоду(РезультатЗапроса);
			Возврат Неопределено;
		
		КонецЕсли;
			
	КонецЕсли;
	
КонецФункции //- ИнфоСофт Стояк Ю.К. 10.02.2023 УА #54628

//+ ИнфоСофт Украинцева А.А. 30.11.2023 УА #62922
Функция ПолучитьВалютуАккаунта(Профиль, ПараметрыЗапроса) Экспорт

	ПараметрыЗапроса.Заголовки.Вставить("Content-Type",   "application/json");		              
	ПараметрыЗапроса.АдресРесурса = "/api/v1/shops/get-currency";
	ПараметрыЗапроса.ФорматОтвета = "Строка";
		
	РезультатЗапроса = ИС_MP_РаботаСHTTPЗапросамиСервер.ОтправитьPOSTЗапрос(ПараметрыЗапроса);
	
	ЗаписатьДанныеОтладки(Профиль, Перечисления.ИС_МП_МетодыHTTP.POST, ПараметрыЗапроса, РезультатЗапроса);
	
	Если РезультатЗапроса.ЕстьОшибки Тогда
		Текст = СтрШаблон("Произошла ошибка при отправке запроса: %1",	РезультатЗапроса.ИнформацияОбОшибке.Описание);
		ОбщегоНазначения.СообщитьПользователю(Текст); 
		Возврат Неопределено;	
	Иначе
		
		Если РезультатЗапроса.КодОтвета = 200 Тогда
			
			JSON = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьОбъектJSON(РезультатЗапроса.Ответ);
			
			Если ТипЗнч(JSON) = Тип("Структура") Тогда				
				Возврат JSON.data.currency;	
			Иначе
				Текст = СтрШаблон("Получен нераспознанный ответ от сервера: %1", РезультатЗапроса.Ответ);
				ОбщегоНазначения.СообщитьПользователю(Текст);
				Возврат Неопределено;
			КонецЕсли;
			
		ИначеЕсли РезультатЗапроса.КодОтвета = 400 Тогда
			
			Текст = СтрШаблон("Неверная форма запроса: %1", РезультатЗапроса.Ответ); 
			ОбщегоНазначения.СообщитьПользователю(Текст);
			Возврат Неопределено;
			
		Иначе
			
			РазобратьHTTPОтветПоКоду(РезультатЗапроса);
			Возврат Неопределено;
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецФункции //- ИнфоСофт Украинцева А.А. 30.11.2023 УА #62922

#КонецОбласти

#Область КарточкиТовара

//+ ИнфоСофт Стояк Ю.К. 10.02.2023 УА #54628
Функция ЗагрузитьКарточкиТоваров(Профиль, ПараметрыЗапроса, ТелоЗапроса) Экспорт

	ПараметрыЗапроса.Заголовки.Вставить("Content-Type", "application/json");		              
	ПараметрыЗапроса.АдресРесурса = "/api/v1/scroll-short-product-by-filter";
	ПараметрыЗапроса.ФорматОтвета = "Строка";
	
	ПараметрыЗапроса.ТелоСтрока = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьСтрокуJSON(ТелоЗапроса);
	
	РезультатЗапроса = ИС_MP_РаботаСHTTPЗапросамиСервер.ОтправитьPOSTЗапрос(ПараметрыЗапроса);
	
	ЗаписатьДанныеОтладки(Профиль, Перечисления.ИС_МП_МетодыHTTP.POST, ПараметрыЗапроса, РезультатЗапроса);
	
	Если РезультатЗапроса.ЕстьОшибки Тогда
		Текст = СтрШаблон("Произошла ошибка при отправке запроса: %1",	РезультатЗапроса.ИнформацияОбОшибке.Описание);
		ОбщегоНазначения.СообщитьПользователю(Текст); 
		Возврат Неопределено;	
	Иначе
	
		Если РезультатЗапроса.КодОтвета = 200 Тогда
		
			JSON = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьОбъектJSON(РезультатЗапроса.Ответ);
			
			Если ТипЗнч(JSON) = Тип("Структура") Тогда				
				
				Если JSON.Свойство("error") И JSON.error = Неопределено Тогда
					Возврат JSON.data;
				Иначе
					Текст = СтрШаблон("Получен нераспознанный ответ от сервера: %1", РезультатЗапроса.Ответ);
			 		ОбщегоНазначения.СообщитьПользователю(Текст);
					Возврат Неопределено;
				КонецЕсли;	
						
			Иначе
				Текст = СтрШаблон("Получен нераспознанный ответ от сервера: %1", РезультатЗапроса.Ответ);
			 	ОбщегоНазначения.СообщитьПользователю(Текст);
				Возврат Неопределено;
			КонецЕсли;
			
		ИначеЕсли РезультатЗапроса.КодОтвета = 400 Тогда
             
            Текст = СтрШаблон("Неверная форма запроса: %1", РезультатЗапроса.Ответ); 
      		ОбщегоНазначения.СообщитьПользователю(Текст);
			Возврат Неопределено;
			
		Иначе
			
			РазобратьHTTPОтветПоКоду(РезультатЗапроса);
			Возврат Неопределено;
		
		КонецЕсли;
			
	КонецЕсли;
	
КонецФункции //- ИнфоСофт Стояк Ю.К. 10.02.2023 УА #54628

//+ ИнфоСофт Стояк Ю.К. 10.02.2023 УА #54628
Функция ПолучитьДанныеОТоваре(Профиль, ПараметрыЗапроса, ТелоЗапроса) Экспорт

	ПараметрыЗапроса.Заголовки.Вставить("Content-Type",   "application/json");		              
	ПараметрыЗапроса.АдресРесурса = "/api/v1/product/get-seller-product";
	ПараметрыЗапроса.ФорматОтвета = "Строка";
	
	ПараметрыЗапроса.ТелоСтрока = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьСтрокуJSON(ТелоЗапроса);
	
	РезультатЗапроса = ИС_MP_РаботаСHTTPЗапросамиСервер.ОтправитьPOSTЗапрос(ПараметрыЗапроса);

	ЗаписатьДанныеОтладки(Профиль, Перечисления.ИС_МП_МетодыHTTP.POST, ПараметрыЗапроса, РезультатЗапроса);
	
	Если РезультатЗапроса.ЕстьОшибки Тогда
		Текст = СтрШаблон("Произошла ошибка при отправке запроса: %1",	РезультатЗапроса.ИнформацияОбОшибке.Описание);
		ОбщегоНазначения.СообщитьПользователю(Текст); 
		Возврат Неопределено;	
	Иначе
	
		Если РезультатЗапроса.КодОтвета = 200 Тогда
		
			JSON = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьОбъектJSON(РезультатЗапроса.Ответ);
			
			Если ТипЗнч(JSON) = Тип("Структура") Тогда				
				Возврат JSON.data;	
			Иначе
				Текст = СтрШаблон("Получен нераспознанный ответ от сервера: %1", РезультатЗапроса.Ответ);
			 	ОбщегоНазначения.СообщитьПользователю(Текст);
				Возврат Неопределено;
			КонецЕсли;
			
		ИначеЕсли РезультатЗапроса.КодОтвета = 400 Тогда
             
            Текст = СтрШаблон("Неверная форма запроса: %1", РезультатЗапроса.Ответ); 
      		ОбщегоНазначения.СообщитьПользователю(Текст);
			Возврат Неопределено;
			
		Иначе
			
			РазобратьHTTPОтветПоКоду(РезультатЗапроса);
			Возврат Неопределено;
		
		КонецЕсли;
			
	КонецЕсли;
	
КонецФункции //- ИнфоСофт Стояк Ю.К. 10.02.2023 УА #54628

//+ ИнфоСофт Стояк Ю.К. 10.04.2024 УА #62574
Функция ПолучитьКатегорииВерхнегоУровня(Профиль, ПараметрыЗапроса) Экспорт

	ПараметрыЗапроса.Заголовки.Вставить("Content-Type",   "application/json");		              
	ПараметрыЗапроса.АдресРесурса = "/api/v1/categories/top";
	ПараметрыЗапроса.ФорматОтвета = "Строка";		
	
	РезультатЗапроса = ИС_MP_РаботаСHTTPЗапросамиСервер.ОтправитьPOSTЗапрос(ПараметрыЗапроса);

	ЗаписатьДанныеОтладки(Профиль, Перечисления.ИС_МП_МетодыHTTP.POST, ПараметрыЗапроса, РезультатЗапроса);
	
	Если РезультатЗапроса.ЕстьОшибки Тогда
		Текст = СтрШаблон("Произошла ошибка при отправке запроса: %1",	РезультатЗапроса.ИнформацияОбОшибке.Описание);
		ОбщегоНазначения.СообщитьПользователю(Текст); 
		Возврат Неопределено;	
	Иначе
	
		Если РезультатЗапроса.КодОтвета = 200 Тогда
		
			JSON = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьОбъектJSON(РезультатЗапроса.Ответ);
			
			Если ТипЗнч(JSON) = Тип("Структура") Тогда				
				Возврат JSON.categories;	
			Иначе
				Текст = СтрШаблон("Получен нераспознанный ответ от сервера: %1", РезультатЗапроса.Ответ);
			 	ОбщегоНазначения.СообщитьПользователю(Текст);
				Возврат Неопределено;
			КонецЕсли;
			
		ИначеЕсли РезультатЗапроса.КодОтвета = 400 Тогда
             
            Текст = СтрШаблон("Неверная форма запроса: %1", РезультатЗапроса.Ответ); 
      		ОбщегоНазначения.СообщитьПользователю(Текст);
			Возврат Неопределено;
			
		Иначе
			
			РазобратьHTTPОтветПоКоду(РезультатЗапроса);
			Возврат Неопределено;
		
		КонецЕсли;
			
	КонецЕсли;
	
КонецФункции //- ИнфоСофт Стояк Ю.К. 10.04.2024 УА #62574

//+ ИнфоСофт Стояк Ю.К. 10.04.2024 УА #62574
Функция ПолучитьДочерниеКатегории(Профиль, ПараметрыЗапроса, ТелоЗапроса) Экспорт

	ПараметрыЗапроса.Заголовки.Вставить("Content-Type",   "application/json");		              
	ПараметрыЗапроса.АдресРесурса = "/api/v1/categories/get";
	ПараметрыЗапроса.ФорматОтвета = "Строка";
	
	ПараметрыЗапроса.ТелоСтрока = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьСтрокуJSON(ТелоЗапроса);
	
	РезультатЗапроса = ИС_MP_РаботаСHTTPЗапросамиСервер.ОтправитьPOSTЗапрос(ПараметрыЗапроса);

	ЗаписатьДанныеОтладки(Профиль, Перечисления.ИС_МП_МетодыHTTP.POST, ПараметрыЗапроса, РезультатЗапроса);
	
	Если РезультатЗапроса.ЕстьОшибки Тогда
		Текст = СтрШаблон("Произошла ошибка при отправке запроса: %1",	РезультатЗапроса.ИнформацияОбОшибке.Описание);
		ОбщегоНазначения.СообщитьПользователю(Текст); 
		Возврат Неопределено;	
	Иначе
	
		Если РезультатЗапроса.КодОтвета = 200 Тогда
		
			JSON = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьОбъектJSON(РезультатЗапроса.Ответ);
			
			Если ТипЗнч(JSON) = Тип("Структура") Тогда				
				Возврат JSON.categories;	
			Иначе
				Текст = СтрШаблон("Получен нераспознанный ответ от сервера: %1", РезультатЗапроса.Ответ);
			 	ОбщегоНазначения.СообщитьПользователю(Текст);
				Возврат Неопределено;
			КонецЕсли;
			
		ИначеЕсли РезультатЗапроса.КодОтвета = 400 Тогда
             
            Текст = СтрШаблон("Неверная форма запроса: %1", РезультатЗапроса.Ответ); 
      		ОбщегоНазначения.СообщитьПользователю(Текст);
			Возврат Неопределено;
			
		Иначе
			
			РазобратьHTTPОтветПоКоду(РезультатЗапроса);
			Возврат Неопределено;
		
		КонецЕсли;
			
	КонецЕсли;
	
КонецФункции //- ИнфоСофт Стояк Ю.К. 10.04.2024 УА #62574

//+ ИнфоСофт Стояк Ю.К. 10.04.2024 УА #62574
Функция ПолучитьЗначенияАтрибутов(Профиль, ПараметрыЗапроса, ТелоЗапроса) Экспорт

	ПараметрыЗапроса.Заголовки.Вставить("Content-Type",   "application/json");		              
	ПараметрыЗапроса.АдресРесурса = "/api/v1/categories/values-dictionary";
	ПараметрыЗапроса.ФорматОтвета = "Строка";
	
	ПараметрыЗапроса.ТелоСтрока = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьСтрокуJSON(ТелоЗапроса);
	
	РезультатЗапроса = ИС_MP_РаботаСHTTPЗапросамиСервер.ОтправитьPOSTЗапрос(ПараметрыЗапроса);

	ЗаписатьДанныеОтладки(Профиль, Перечисления.ИС_МП_МетодыHTTP.POST, ПараметрыЗапроса, РезультатЗапроса);
	
	Если РезультатЗапроса.ЕстьОшибки Тогда
		Текст = СтрШаблон("Произошла ошибка при отправке запроса: %1",	РезультатЗапроса.ИнформацияОбОшибке.Описание);
		ОбщегоНазначения.СообщитьПользователю(Текст); 
		Возврат Неопределено;	
	Иначе
	
		Если РезультатЗапроса.КодОтвета = 200 Тогда
		
			JSON = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьОбъектJSON(РезультатЗапроса.Ответ);
			
			Если ТипЗнч(JSON) = Тип("Структура") Тогда				
				Возврат JSON.values;	
			Иначе
				Текст = СтрШаблон("Получен нераспознанный ответ от сервера: %1", РезультатЗапроса.Ответ);
			 	ОбщегоНазначения.СообщитьПользователю(Текст);
				Возврат Неопределено;
			КонецЕсли;
			
		ИначеЕсли РезультатЗапроса.КодОтвета = 400 Тогда
             
            Текст = СтрШаблон("Неверная форма запроса: %1", РезультатЗапроса.Ответ); 
      		ОбщегоНазначения.СообщитьПользователю(Текст);
			Возврат Неопределено;
			
		Иначе
			
			РазобратьHTTPОтветПоКоду(РезультатЗапроса);
			Возврат Неопределено;
		
		КонецЕсли;
			
	КонецЕсли;
	
КонецФункции //- ИнфоСофт Стояк Ю.К. 10.04.2024 УА #62574

//+ ИнфоСофт Стояк Ю.К. 10.04.2024 УА #62574
Функция СоздатьКТ(Профиль, ПараметрыЗапроса) Экспорт

	ПараметрыЗапроса.Заголовки.Вставить("Content-Type",   "application/json");		              
	ПараметрыЗапроса.АдресРесурса = "/api/v1/product/create";
	ПараметрыЗапроса.ФорматОтвета = "Строка";		
	
	РезультатЗапроса = ИС_MP_РаботаСHTTPЗапросамиСервер.ОтправитьPOSTЗапрос(ПараметрыЗапроса);

	ЗаписатьДанныеОтладки(Профиль, Перечисления.ИС_МП_МетодыHTTP.POST, ПараметрыЗапроса, РезультатЗапроса);
	
	Если РезультатЗапроса.ЕстьОшибки Тогда
		Текст = СтрШаблон("Произошла ошибка при отправке запроса: %1",	РезультатЗапроса.ИнформацияОбОшибке.Описание);
		ОбщегоНазначения.СообщитьПользователю(Текст); 
		Возврат Неопределено;	
	Иначе
	
		Если РезультатЗапроса.КодОтвета = 200 Тогда
		
			JSON = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьОбъектJSON(РезультатЗапроса.Ответ);
			
			Если ТипЗнч(JSON) = Тип("Структура") Тогда				
				Возврат JSON.results;	
			Иначе
				Текст = СтрШаблон("Получен нераспознанный ответ от сервера: %1", РезультатЗапроса.Ответ);
			 	ОбщегоНазначения.СообщитьПользователю(Текст);
				Возврат Неопределено;
			КонецЕсли;
			
		ИначеЕсли РезультатЗапроса.КодОтвета = 400 Тогда
             
            Текст = СтрШаблон("Неверная форма запроса: %1", РезультатЗапроса.Ответ); 
      		ОбщегоНазначения.СообщитьПользователю(Текст);
			Возврат Неопределено;
			
		Иначе
			
			РазобратьHTTPОтветПоКоду(РезультатЗапроса);
			Возврат Неопределено;
		
		КонецЕсли;
			
	КонецЕсли;
	
КонецФункции //- ИнфоСофт Стояк Ю.К. 10.04.2024 УА #6257

#КонецОбласти
 
#Область MarketplaceСборочныеЗадания

#Область Заказы

//+ ИнфоСофт Стояк Ю.К. 27.02.2024 УА #56408
Функция ПолучитьСписокЗаказов(Профиль, ПараметрыЗапроса, ТелоЗапроса) Экспорт

	ПараметрыЗапроса.Заголовки.Вставить("Content-Type",   "application/json");		              
	ПараметрыЗапроса.АдресРесурса = "/seller-api/v1/order/get-order-list";
	ПараметрыЗапроса.ФорматОтвета = "Строка";
	
	ПараметрыЗапроса.ТелоСтрока = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьСтрокуJSON(ТелоЗапроса);
	
	РезультатЗапроса = ИС_MP_РаботаСHTTPЗапросамиСервер.ОтправитьPOSTЗапрос(ПараметрыЗапроса); 
	
	//+ ИнфоСофт Заварин К.В. 24.05.24 УА #76506
	ЗаписатьДанныеОтладки(Профиль, Перечисления.ИС_МП_МетодыHTTP.POST, ПараметрыЗапроса, РезультатЗапроса);
	//- ИнфоСофт Заварин К.В. 24.05.24 УА #76506
	
	Если РезультатЗапроса.ЕстьОшибки Тогда
		Текст = СтрШаблон("Произошла ошибка при отправке запроса: %1",	РезультатЗапроса.ИнформацияОбОшибке.Описание);
		ОбщегоНазначения.СообщитьПользователю(Текст); 
		Возврат Неопределено;	
	Иначе
	
		Если РезультатЗапроса.КодОтвета = 200 Тогда
		
			JSON = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьОбъектJSON(РезультатЗапроса.Ответ, Истина);
			
			Если ТипЗнч(JSON) = Тип("Соответствие") Тогда				
				Возврат JSON["data"];		
			Иначе
				Текст = СтрШаблон("Получен нераспознанный ответ от сервера: %1", РезультатЗапроса.Ответ);
			 	ОбщегоНазначения.СообщитьПользователю(Текст);
				Возврат Неопределено;
			КонецЕсли;
			
		ИначеЕсли РезультатЗапроса.КодОтвета = 400 Тогда
             
            Текст = СтрШаблон("Неверная форма запроса: %1", РезультатЗапроса.Ответ); 
      		ОбщегоНазначения.СообщитьПользователю(Текст);
			Возврат Неопределено;
			
		Иначе
			
			РазобратьHTTPОтветПоКоду(РезультатЗапроса);
			Возврат Неопределено;
		
		КонецЕсли;
			
	КонецЕсли;
	
КонецФункции //- ИнфоСофт Стояк Ю.К. 27.02.2024 УА #56408

//+ ИнфоСофт Стояк Ю.К. 14.06.2024 УА #62573
Функция ПолучитьСписокОтправлений(Профиль, ПараметрыЗапроса, ТелоЗапроса) Экспорт

	ПараметрыЗапроса.Заголовки.Вставить("Content-Type",   "application/json");		              
	ПараметрыЗапроса.АдресРесурса = "/seller-api/v1/logistic-order/get";
	ПараметрыЗапроса.ФорматОтвета = "Строка";
	
	ПараметрыЗапроса.ТелоСтрока = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьСтрокуJSON(ТелоЗапроса);
	
	РезультатЗапроса = ИС_MP_РаботаСHTTPЗапросамиСервер.ОтправитьPOSTЗапрос(ПараметрыЗапроса); 
	
	//+ ИнфоСофт Заварин К.В. 24.05.24 УА #76506
	ЗаписатьДанныеОтладки(Профиль, Перечисления.ИС_МП_МетодыHTTP.POST, ПараметрыЗапроса, РезультатЗапроса);
	//- ИнфоСофт Заварин К.В. 24.05.24 УА #76506
	
	Если РезультатЗапроса.ЕстьОшибки Тогда
		Текст = СтрШаблон("Произошла ошибка при отправке запроса: %1",	РезультатЗапроса.ИнформацияОбОшибке.Описание);
		ОбщегоНазначения.СообщитьПользователю(Текст); 
		Возврат Неопределено;	
	Иначе
	
		Если РезультатЗапроса.КодОтвета = 200 Тогда
		
			JSON = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьОбъектJSON(РезультатЗапроса.Ответ, Истина);
			
			Если ТипЗнч(JSON) = Тип("Соответствие") Тогда				
				Возврат JSON["data"];		
			Иначе
				Текст = СтрШаблон("Получен нераспознанный ответ от сервера: %1", РезультатЗапроса.Ответ);
			 	ОбщегоНазначения.СообщитьПользователю(Текст);
				Возврат Неопределено;
			КонецЕсли;
			
		ИначеЕсли РезультатЗапроса.КодОтвета = 400 Тогда
             
            Текст = СтрШаблон("Неверная форма запроса: %1", РезультатЗапроса.Ответ); 
      		ОбщегоНазначения.СообщитьПользователю(Текст);
			Возврат Неопределено;
			
		Иначе
			
			РазобратьHTTPОтветПоКоду(РезультатЗапроса);
			Возврат Неопределено;
		
		КонецЕсли;
			
	КонецЕсли;
	
КонецФункции //- ИнфоСофт Стояк Ю.К. 14.06.2024 УА #62573  


//+ ИнфоСофт Стояк Ю.К. 14.06.2024 УА #62573
Функция ВыгрузитьКиЗ(Профиль, ПараметрыЗапроса, ТелоЗапроса) Экспорт

	ПараметрыЗапроса.Заголовки.Вставить("Content-Type",   "application/json");		              
	ПараметрыЗапроса.АдресРесурса = "/seller-api/v1/logistic-order/check-marking";
	ПараметрыЗапроса.ФорматОтвета = "Строка";
	
	ПараметрыЗапроса.ТелоСтрока = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьСтрокуJSON(ТелоЗапроса);
	
	РезультатЗапроса = ИС_MP_РаботаСHTTPЗапросамиСервер.ОтправитьPOSTЗапрос(ПараметрыЗапроса); 
	
	//+ ИнфоСофт Заварин К.В. 24.05.24 УА #76506
	ЗаписатьДанныеОтладки(Профиль, Перечисления.ИС_МП_МетодыHTTP.POST, ПараметрыЗапроса, РезультатЗапроса);
	//- ИнфоСофт Заварин К.В. 24.05.24 УА #76506
	
	Если РезультатЗапроса.ЕстьОшибки Тогда
		Текст = СтрШаблон("Произошла ошибка при отправке запроса: %1",	РезультатЗапроса.ИнформацияОбОшибке.Описание);
		ОбщегоНазначения.СообщитьПользователю(Текст); 
		Возврат Неопределено;	
	Иначе
	
		Если РезультатЗапроса.КодОтвета = 200 Тогда
		
			JSON = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьОбъектJSON(РезультатЗапроса.Ответ);
			
			Если ТипЗнч(JSON) = Тип("Структура") Тогда				
				Возврат JSON.data;		
			Иначе
				Текст = СтрШаблон("Получен нераспознанный ответ от сервера: %1", РезультатЗапроса.Ответ);
			 	ОбщегоНазначения.СообщитьПользователю(Текст);
				Возврат Неопределено;
			КонецЕсли;
			
		ИначеЕсли РезультатЗапроса.КодОтвета = 400 Тогда
             
            Текст = СтрШаблон("Неверная форма запроса: %1", РезультатЗапроса.Ответ); 
      		ОбщегоНазначения.СообщитьПользователю(Текст);
			Возврат Неопределено;
			
		Иначе
			
			РазобратьHTTPОтветПоКоду(РезультатЗапроса);
			Возврат Неопределено;
		
		КонецЕсли;
			
	КонецЕсли;
	
КонецФункции //- ИнфоСофт Стояк Ю.К. 14.06.2024 УА #62573 

#КонецОбласти

#КонецОбласти

#Область MarketplaceОстатки

#Область Склады
	
#КонецОбласти

#Область Остатки

//+ ИнфоСофт Стояк Ю.К. 10.02.2023 УА #54628
Функция ВыгрузитьОстатки(Профиль, ПараметрыЗапроса, СтруктураТела) Экспорт

	ПараметрыЗапроса.Заголовки.Вставить("Content-Type",   "application/json");		              
	ПараметрыЗапроса.АдресРесурса = "/api/v1/product/update-sku-stock";
	ПараметрыЗапроса.ФорматОтвета = "Строка";
	
	ТелоЗапроса = Новый Структура;
	ТелоЗапроса.Вставить("products", СтруктураТела);
	
	ПараметрыЗапроса.ТелоСтрока = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьСтрокуJSON(ТелоЗапроса);
		
	РезультатЗапроса = ИС_MP_РаботаСHTTPЗапросамиСервер.ОтправитьPOSTЗапрос(ПараметрыЗапроса);
	
	ЗаписатьДанныеОтладки(Профиль, Перечисления.ИС_МП_МетодыHTTP.POST, ПараметрыЗапроса, РезультатЗапроса);
	
	Если РезультатЗапроса.ЕстьОшибки Тогда
		Текст = СтрШаблон("Произошла ошибка при отправке запроса: %1",	РезультатЗапроса.ИнформацияОбОшибке.Описание);
		ОбщегоНазначения.СообщитьПользователю(Текст); 
		Возврат Неопределено;	
	Иначе
	
		Если РезультатЗапроса.КодОтвета = 200 Тогда
		
			JSON = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьОбъектJSON(РезультатЗапроса.Ответ);
			
			Если ТипЗнч(JSON) = Тип("Структура") Тогда				
				Возврат JSON.results[0].ok;	
			Иначе
				Текст = СтрШаблон("Получен нераспознанный ответ от сервера: %1", РезультатЗапроса.Ответ);
			 	ОбщегоНазначения.СообщитьПользователю(Текст);
				Возврат Неопределено;
			КонецЕсли;
			
		ИначеЕсли РезультатЗапроса.КодОтвета = 400 Тогда
             
            Текст = СтрШаблон("Неверная форма запроса: %1", РезультатЗапроса.Ответ); 
      		ОбщегоНазначения.СообщитьПользователю(Текст);
			Возврат Неопределено;
			
		Иначе
			
			РазобратьHTTPОтветПоКоду(РезультатЗапроса);
			Возврат Неопределено;
		
		КонецЕсли;
			
	КонецЕсли;
	
КонецФункции //- ИнфоСофт Стояк Ю.К. 10.02.2023 УА #54628

//+ ИнфоСофт Стояк Ю.К. 02.07.2023 УА #59330
Функция ПолучитьОстаткиТовара(Профиль, ПараметрыЗапроса, СтруктураТела) Экспорт

	ПараметрыЗапроса.Заголовки.Вставить("Content-Type",   "application/json");		              
	ПараметрыЗапроса.АдресРесурса = "/api/v1/stocks";
	ПараметрыЗапроса.ФорматОтвета = "Строка";
	
	ТелоЗапроса = Новый Структура;
	ТелоЗапроса.Вставить("stocks", СтруктураТела);
	
	ПараметрыЗапроса.ТелоСтрока = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьСтрокуJSON(ТелоЗапроса);
		
	РезультатЗапроса = ИС_MP_РаботаСHTTPЗапросамиСервер.ОтправитьPOSTЗапрос(ПараметрыЗапроса);
	
	ЗаписатьДанныеОтладки(Профиль, Перечисления.ИС_МП_МетодыHTTP.POST, ПараметрыЗапроса, РезультатЗапроса);
	
	Если РезультатЗапроса.ЕстьОшибки Тогда
		Текст = СтрШаблон("Произошла ошибка при отправке запроса: %1",	РезультатЗапроса.ИнформацияОбОшибке.Описание);
		ОбщегоНазначения.СообщитьПользователю(Текст); 
		Возврат Неопределено;	
	Иначе
	
		Если РезультатЗапроса.КодОтвета = 200 Тогда
		
			JSON = ИС_MP_РаботаСHTTPЗапросамиСервер.ПолучитьОбъектJSON(РезультатЗапроса.Ответ);
			
			Если ТипЗнч(JSON) = Тип("Структура") Тогда				
				Возврат JSON.data;	
			Иначе
				Текст = СтрШаблон("Получен нераспознанный ответ от сервера: %1", РезультатЗапроса.Ответ);
			 	ОбщегоНазначения.СообщитьПользователю(Текст);
				Возврат Неопределено;
			КонецЕсли;
			
		ИначеЕсли РезультатЗапроса.КодОтвета = 400 Тогда
             
            Текст = СтрШаблон("Неверная форма запроса: %1", РезультатЗапроса.Ответ); 
      		ОбщегоНазначения.СообщитьПользователю(Текст);
			Возврат Неопределено;
			
		Иначе
			
			РазобратьHTTPОтветПоКоду(РезультатЗапроса);
			Возврат Неопределено;
		
		КонецЕсли;
			
	КонецЕсли;
	
КонецФункции //- ИнфоСофт Стояк Ю.К. 02.07.2023 УА #59330

#КонецОбласти

#КонецОбласти


#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

//+ ИнфоСофт Стояк Ю.К. 31.01.2023 УА #54628
Процедура РазобратьHTTPОтветПоКоду(СтруктураОтвета)

	Если СтруктураОтвета.КодОтвета = 400 Тогда
		
		Текст = СтрШаблон("Передан неверный параметр: %1", СтруктураОтвета.Ответ);
		ОбщегоНазначения.СообщитьПользователю(Текст);
		
	ИначеЕсли СтруктураОтвета.КодОтвета = 403 Тогда
		
		Текст = СтрШаблон("Произошла ошибка доступа (Доступ запрещен)
						  |Возможно используется пустой или отозванный токен. 
						  |Проверьте настройки авторизации : %1", СтруктураОтвета.Ответ);
		ОбщегоНазначения.СообщитьПользователю(Текст); 
		
	ИначеЕсли СтруктураОтвета.КодОтвета = 401 Тогда
		
		Текст = СтрШаблон("Произошла ошибка авторизации: %1", СтруктураОтвета.Ответ);
		ОбщегоНазначения.СообщитьПользователю(Текст);
		
	ИначеЕсли СтруктураОтвета.КодОтвета = 503 Тогда
		
		Текст = СтрШаблон("Сервер временно недоступен из-за высокой загрузки.: %1", СтруктураОтвета.Ответ);
		ОбщегоНазначения.СообщитьПользователю(Текст);
						
	Иначе
		Текст = СтрШаблон("Произошла неизвестная ошибка, код ответа: %1, ошибка: %2", 
			СтруктураОтвета.КодОтвета, СтруктураОтвета.Ответ);			
		ОбщегоНазначения.СообщитьПользователю(Текст);				
	КонецЕсли;

КонецПроцедуры //- ИнфоСофт Стояк Ю.К. 31.01.2023 УА #54628

//+ ИнфоСофт Ивонин К.А. 27.06.22 УА #46999
Процедура ЗаписатьДанныеОтладки(Профиль, Метод, ПараметрыЗапроса, РезультатЗапроса)
       
	Если Не Профиль.РежимОтладки Тогда
		Возврат;
	КонецЕсли;
		
	ИС_MP_Сервер.ЗаписатьДанныеОтладки(Профиль, Перечисления.ИС_МП_Маркетплейсы.Али, Метод, ПараметрыЗапроса, РезультатЗапроса);	

КонецПроцедуры //- ИнфоСофт Ивонин К.А. 27.06.22 УА #46999

#КонецОбласти
